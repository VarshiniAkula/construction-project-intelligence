# Core requirements — lightweight for Vercel serverless deployment
# For local dev with full features (embeddings, pymupdf), also install requirements-local.txt

# Web framework
fastapi>=0.115,<1.0
python-multipart>=0.0.9

# Supabase client
supabase>=2.0

# Validation / settings
pydantic>=2.6
pydantic-settings>=2.4

# Auth
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7
bcrypt>=4.0,<5.0

# PDF processing (lightweight — ~2MB)
pypdf>=4.0

# Document parsing
python-docx>=1.0
openpyxl>=3.1

# HTTP client
httpx>=0.27
