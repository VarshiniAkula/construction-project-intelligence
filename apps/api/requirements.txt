# Web framework
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
python-multipart>=0.0.9

# Database (Supabase PostgreSQL)
sqlalchemy[asyncio]>=2.0
asyncpg>=0.30
psycopg[binary]>=3.2

# Validation / settings
pydantic>=2.6
pydantic-settings>=2.4

# Auth
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7
bcrypt>=4.0,<5.0

# PDF processing
pymupdf>=1.24

# Document parsing
python-docx>=1.0
openpyxl>=3.1

# HTTP client for AI endpoints + Supabase Storage
httpx>=0.27

# Lightweight local embeddings (ONNX, ~100MB vs 2GB for torch)
fastembed>=0.4

# Testing
pytest>=8.0
pytest-asyncio>=0.24
