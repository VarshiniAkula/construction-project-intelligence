# Slim requirements for Vercel serverless (excludes heavy ML packages)
# Uses pypdf (~2MB) instead of pymupdf (~50MB), no fastembed (~200MB)
fastapi>=0.115,<1.0
python-multipart>=0.0.9
supabase>=2.0
pydantic>=2.6
pydantic-settings>=2.4
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7
bcrypt>=4.0,<5.0
httpx>=0.27
# Lightweight PDF text extraction
pypdf>=4.0
# Document parsing
python-docx>=1.0
openpyxl>=3.1
